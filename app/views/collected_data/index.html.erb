<h1>Listing collected_datas</h1>

<table>
  <tr>
    <th>Script target</th>
    <th>Executed at</th>
  </tr>

<% @collected_data.each do |collected_datum| %>
  <tr>
    <td><%= link_to collected_datum.script_target.name, collected_datum %></td>
    <td><%= collected_datum.created_at %></td>
  </tr>
<% end %>
</table>

<br />
